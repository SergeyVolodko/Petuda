<UserControl x:Class="Petuda.Views.Controls.DragJokePopupContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Border Style="{StaticResource DraggingJokePopupBorderStyle}">
        <StackPanel Orientation="Horizontal" Margin="5">

            <Image Source="../Images/puzzle.png"
                   Width="34"
                   Height="34" />
            <StackPanel Margin="10,0,0,0">
                <Label Content="{Binding Joke.Name}" Height="30" />
                <TextBlock Style="{StaticResource DraggingJokePopupTextBlockStyle}"
                           Text="{Binding Joke.Text}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
